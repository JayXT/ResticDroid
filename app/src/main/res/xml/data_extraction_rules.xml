<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device through Android's own backup transport.
  The encrypted credential blob is useless elsewhere anyway — the key that
  decrypts it lives in this device's keystore and cannot be exported — but
  shipping it to a cloud backup would be pointless noise at best.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
